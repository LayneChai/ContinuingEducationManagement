spring:
  application:
    name: continue-education-backend
  profiles:
    active: dev

server:
  port: 8080

springdoc:
  api-docs:
    enabled: true

knife4j:
  enable: true
  setting:
    language: zh_cn
